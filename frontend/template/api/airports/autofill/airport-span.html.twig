<span class="hidden">{{ airport_code }}|{{ airport_name }}|{{ city_name }}|{{ airport_country }}</span>
<span class="list-group-item list-group-item-action d-flex gap-3 py-0">
<i class="fas fa-lg fa-plane-departure pt-1"></i>
<div class="d-flex gap-2 w-100 justify-content-between">
<div class="small">
<span class="mb-0"><strong>{{ airport_name }}</strong></span>
<p class="mb-0 opacity-75 small">{{ city_name }}, {{ airport_country }}</p>
</div>
<small class="opacity-50 text-nowrap">{{ airport_code }}</small>
</div>
</span>
