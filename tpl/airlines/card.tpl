<div class="feature col mb-4">
    <div class="feature-icon d-inline-flex align-items-center justify-content-center fs-2 mb-3">
        <img src="{{ airline-logo-img }}" class="rounded-circle me-3" alt="{{ airline-title }}" title="{{ airline-title }}" />
    </div>
    <h3 class="fs-2">{{ airline-title }}</h3>
    <p>Daily traffic: {{ airline-traffic }} passengers</p>
    <button type="button" class="btn btn-link empty-link ps-0">
        Official website
        <i class="fa-solid fa-chevron-right ps-1"></i>
    </button>
</div>

