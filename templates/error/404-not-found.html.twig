{% extends 'layout.html.twig' %}

{% block title %}Page not found{% endblock %}

{% block content %}
{# The same local path and cache-buster main.css uses. This asked the CDN for
   it, so `frontend/css/404.css` was a file nothing could reach: editing it
   changed nothing, and the deployed copy was the only one that counted. #}
<link href="{{ asset(config('site.directory.css') ~ '/404.css') }}" rel="stylesheet" media="all" />
{# The landmark this page did not have. Around `.wrapper` and not around the
   whole block, so the stylesheet link above and the script below stay outside
   it -- neither is content, and a <link> in a landmark is metadata in the
   wrong place.

   Inert to look at: `.wrapper` is `width: 100%` of its parent and the new
   <main> is a plain block filling the same box, so nothing about the flex
   centring inside it changes. #}
<main role="main">
<div class="wrapper">
    <div class="text_group">
        <p class="text_404">404</p>
        <p class="text_lost">The page you are looking for <br />has been lost in the sky.</p>
        {# The body offered nothing to do -- only the header nav, which is not
           where anyone looks after landing on a 404. Same primary action the
           empty states use, because it is the same situation: nothing here,
           here is the thing to do instead. #}
        <a href="/" class="btn btn-primary mt-3">Search for flights</a>
    </div>
    <div class="window_group">
        <div class="window_404">
            <div class="stars"></div>
        </div>
    </div>
</div>
</main>
<script>
    let starContainer = document.querySelector(".stars");

    for (let i = 0; i < 100; i++) {
        starContainer.innerHTML += '<div class="star"></div>';
    }
</script>
{% endblock %}
